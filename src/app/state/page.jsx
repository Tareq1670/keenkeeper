import StatusPage from "@/Components/State";

export const metadata = {
    title: "KeenKeeper || State Analytics Page",
    description: "Al Data Analytics Page",
};

const StatusMainPage = () => {
    return (
        <div>
            <StatusPage />
        </div>
    );
};

export default StatusMainPage;
